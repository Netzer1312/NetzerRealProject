<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:background="@drawable/background_gradient_game"
    tools:context=".Real_GameReg">

    <!-- שם העיר -->
    <TextView
        android:id="@+id/TextName_CityOrState"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="San Francisco"
        android:textSize="48sp"
        android:textStyle="bold"
        android:textColor="#FFEB3B"
        android:shadowColor="#66000000"
        android:shadowDx="2"
        android:shadowDy="2"
        android:shadowRadius="4"
        android:layout_marginBottom="32dp"/>

    <!-- כפתור CITY -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/City"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="CITY"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:background="@drawable/teal_flat"
        android:layout_marginBottom="18dp"
        android:padding="14dp"
        android:backgroundTint="@null"/>

    <!-- כפתור STATE -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/State"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="STATE"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:background="@drawable/teal_flat"
        android:layout_marginBottom="36dp"
        android:padding="14dp"
        android:backgroundTint="@null"/>

    <!-- ניקוד -->
    <TextView
        android:id="@+id/textScore"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0"
        android:textSize="40sp"
        android:textStyle="bold"
        android:textColor="#B3E5FC"
        android:layout_marginBottom="12dp"/>

    <!-- טיימר -->
    <TextView
        android:id="@+id/tvCdt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="10s"
        android:textSize="40sp"
        android:textStyle="bold"
        android:textColor="#03A9F4"
        android:layout_marginBottom="28dp"/>

    <!-- רמז מזג אוויר -->
    <TextView
        android:id="@+id/WeatherHint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Loading the weather..."
        android:textSize="20sp"
        android:textColor="#FFFFFF"
        android:gravity="center"
        android:padding="12dp"
        android:background="#607D8B"
        android:layout_marginBottom="28dp"/>

    <!-- כפתור קריינות -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/SpeakButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🔊 Listen to Name 🔊"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:background="@drawable/rounded_button_teal"
        android:padding="14dp"
        android:backgroundTint="@null"/>
</LinearLayout>

